# Model

This folder contains the machine-learning component of the project.

The current application uses a transparent skill-matching algorithm.

The `train_model.py` file is prepared for adding a machine-learning model in a future version.

Possible algorithms:

- Decision Tree
- Random Forest
- K-Nearest Neighbors
- Logistic Regression